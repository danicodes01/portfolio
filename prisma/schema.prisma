generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("POSTGRES_PRISMA_URL")
  directUrl = env("POSTGRES_URL_NON_POOLING")
}

model Project {
  id            String     @id @default(cuid())
  slug          String     @unique
  title         String
  
  // Localized content fields
  summary       String
  summaryEs     String?    @map("summary_es")
  
  info          String
  infoEs        String?    @map("info_es")
  
  link          String
  repo          String
  media         String[]
  
  date          DateTime
}